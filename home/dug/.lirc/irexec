# LIRCRC Auto Generated by Mythbuntu Lirc Generator
# Author(s): Mario Limonciello, Nick Fox, John Baab
# Created for use with Mythbuntu
begin
    remote = devinput
    prog = irexec
    button = KEY_SLEEP
    config = (curl --insecure -X POST -H "x-ha-access: XXXX" https://localhost:8123/api/services/switch/toggle -d '{ "entity_id": "light.lounge_side_lights" }')

    repeat = 0
    delay = 0
end

begin
    remote = devinput
    prog = irexec
    button = KEY_SUBTITLE
    config = /sbin/reboot
    repeat = 0
    delay = 0
end

begin
    remote = devinput
    prog = irexec
    button = KEY_DVD
    config = (curl --insecure -X POST -H "x-ha-access: XXXX" https://localhost:8123/api/services/light/toggle -d '{ "entity_id": "light.lounge_side_lights" }')
    repeat = 0
    delay = 0
end

# begin
#     remote = devinput
#     prog = irexec
#     button = KEY_PVR
#     config = (kill $(pgrep mythfrontend) || true) && mythfrontend --service &
#     repeat = 0
#     delay = 0
# end

